RULES:
  - ALWAYS FOLLOW THE FLOW OF THIS PROJECT THE "PLAN & BUILD FOLDER".

Now here is my 1 to 8 .md.

You will make a checklist here once they are completed.

1 [x] - App Architecture & Strategy Setup (Unit Economics & Frontend Types)
2 [x] - Setup Core Modular Apps (`users`, `vendors`, `catalog`) & Models
3 [x] - Infrastructure Setup (PostGIS - SKIPPED, Channels/Redis - DONE, Celery - DONE)
4 [x] - Build `orders` App (UUIDs, Order & OrderItem models, Statuses)
5 [x] - Build `payments` App (Payment Transactions, Auditable Ledger)
6 [x] - API & Webhooks (`CheckoutView`, `PayMongoWebhookView` with atomicity)
7 [x] - Real-Time Engine (ASGI, Django Channels Consumers for Merchant Alerts)
8 [x] - MVP Finalization & Security Check

---

## Road Map for "Version 1.1"

9 [ ] - **Rider Dispatch System**: Build a dispatch logic that pings the nearest online riders via WebSockets when an order is accepted by a merchant.
10 [ ] - **Automated Cancellation (Safety Net)**: Use Celery to monitor orders. If an order remains in `PENDING` for more than 10 minutes, auto-cancel it and notify the customer.
11 [ ] - **Geospatial Optimization (PostGIS)**: Transition from `DecimalField` to `PointField` once GDAL is available for native PostgreSQL radius searches.
